// server bootstrapper
export const SERVER_BOOTSTRAPPER = Symbol('ServerBootstrapper');

// config stores
export const SERVER_CONFIG_STORE = Symbol('ServerConfigStore');

// middlewares
export const MIDDLEWARE_FACTORY = Symbol('MiddlewareFactory');
